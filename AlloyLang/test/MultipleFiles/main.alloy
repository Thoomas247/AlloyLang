import lib::list;

extern printf (const str: String, ...) -> i64;

fn main() -> i64
{
	var list: List(i32) = List(i32):create();

	list:add(32);
	list:add(45);

	printf("list.arr[1] = %d", list.arr[1]);

	return list.arr[1];
}