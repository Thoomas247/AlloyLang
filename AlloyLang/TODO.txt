TODO for version 0.1 release:
	[] working generics for functions, types and generic member functions on generic types
	[x] working enums with payloads, including value-capturing in switch and if statements
	[x] built-in cast functions for built-in types (f32:to_u32(), u64:to_u8(), ...)
	[] global variables
	[] array sizes as part of arrays
	[] allow arrays allocated on the heap to be empty (ie allow 'var arr: *[T] = new {};')
	[] ensure '&' operator is implemented properly, see references examples in AlloyLang.txt
	[x] 'import ... as ...;'
	[x] variable type inferring at definition

TODO for version 0.2 release:
	[] 'pub' and 'exp' at struct member level
	[] member function definitions should only be allowed in the file where the type has been defined
	[] traits to specify functions which a type must implement, see traits examples in AlloyLang.txt
	[] annotations to add information to types, such as which traits they implement
	[] restrictions on generic parameters using traits
	[] traits as interfaces, see traits examples in AlloyLang.txt
	[] macros, see macros examples in AlloyLang.txt
	[] operator overloading (including destructor)