- How do we insert CR or LF in a string ? printf("\n") outputs: \n

- Need to add support for functions with variable arguments using ...

- A return statement with no return value causes an "Invalid ID" error, e.g.:
	fn process(const a : &i64) {
		printf("a = %f\n", a);
		return;
	}

- Need support for hexadecimal, octal, binary ... integers

- This statement is not parsed correctly: 
	var test : i64;

